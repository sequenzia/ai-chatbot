# AI Chatbot Environment Configuration
# Copy this file to .env.local and fill in your values

# =============================================================================
# Required
# =============================================================================

# OpenAI API key
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Tavily API key for web search functionality
# Get your key at https://tavily.com
# Note: Web search is disabled by default. Set WEB_SEARCH_ENABLED=true to enable.
TAVILY_API_KEY=your_tavily_api_key_here

# =============================================================================
# Optional - AI Model Configuration
# =============================================================================

# OpenAI API mode
# - 'chat-completions': Use Chat Completions API (default, standard mode)
# - 'responses': Use Responses API (newer API with additional features)
# OPENAI_API_MODE=chat-completions

# Default model for chat conversations
# Available: gpt-oss-120b, gpt-4o, gpt-4o-mini
# NEXT_PUBLIC_AI_DEFAULT_MODEL=gpt-4o

# Model used for generating conversation titles
# AI_TITLE_MODEL=gpt-4o-mini

# Maximum length for generated titles (in characters)
# AI_TITLE_MAX_LENGTH=50

# Timeout for title generation requests (in milliseconds)
# AI_TITLE_TIMEOUT_MS=10000

# =============================================================================
# Optional - Tools
# =============================================================================

# Enable web search tool (requires TAVILY_API_KEY)
# Set to 'true' to enable, disabled by default
# WEB_SEARCH_ENABLED=true

# =============================================================================
# Optional - Backend Configuration
# =============================================================================

# Custom API backend URL
# When set, the frontend will connect to this URL instead of the built-in API.
# Useful for frontend-only deployments connecting to a separate backend server.
# The URL should be the base URL without trailing slash.
# Endpoints /chat and /generate-title will be appended automatically.
# Example: NEXT_PUBLIC_CHAT_API_URL=https://api.example.com
# NEXT_PUBLIC_CHAT_API_URL=

# Agent identifier for chat API requests
# When set, includes "agent" parameter in request body for routing.
# When not set, the agent parameter is omitted entirely from requests.
# Useful for backends that support multiple agents (e.g., Mamba Server).
# NEXT_PUBLIC_AI_AGENT=main

# =============================================================================
# Optional - Development / Debugging
# =============================================================================

# Enable AI SDK DevTools for debugging (development only)
# Run `npx @ai-sdk/devtools` to view at http://localhost:4983
# AI_DEBUG_ON=true

